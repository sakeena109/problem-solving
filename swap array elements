class Solution {
  public:
    // Function to swap elements of the array
    void swapElements(vector<int> &arr) {
        // Your code here
        for(int i=0;i<arr.size();i++){
            swap(arr[i],arr[i+2])
        }
        return swap;
        
    }
};
