class Solution {
  public:
    // arr[] is the array
    long long int product(vector<int> &arr) {
        long long result = 1;
        int mod = 1000000007;

        for (int num : arr) {
            result = (result * num) % mod;
        }

        return result;
    }
};
