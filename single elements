class Solution {
  public:
    int getSingle(vector<int>& arr) {
        int result = 0;
        for (int num : arr) {
            result ^= num;  // XOR all elements
        }
        return result;  // The number that occurs odd times
    }
};
